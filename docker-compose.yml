services:
  ha-fadecandy:
    build:
      context: ./fadecandy-node
      args:
        BUILD_FROM: ghcr.io/home-assistant/aarch64-base-python:latest
    ports:
      - "7890:7890"
    volumes:
      - ./fadecandy-node/src/:/usr/src/app/src/